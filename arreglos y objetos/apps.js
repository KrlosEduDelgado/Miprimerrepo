// arreglos  arrays (matrices)
const frutas = ["fresa", "manzana", "pera" ]; 
const numeros = [1, 2, 3, 4]; /* los elementos en un arreglo (string) van separados por una coma si no existe la coma se entiende que son juntos, por ejemplo la pera con la manzana */

console.log(frutas [2]) /* me va a devolver a pera por que esa es su posicion  empieza siempre desde el cero */
